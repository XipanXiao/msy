RewriteEngine on
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R,L]

RewriteCond %{REQUEST_URI} /index.html$
RewriteCond %{REQUEST_URI} ^/$
RewriteRule .* order.html [R,L]
